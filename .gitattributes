# Normalize line endings. Without this, editing a file on Windows rewrites every
# line to CRLF and git reports the whole file as changed.
* text=auto eol=lf

# Never touch binaries.
*.mp3 binary
*.png binary
*.jpg binary
*.ico binary
